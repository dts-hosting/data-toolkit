proxy:
  ssl: true
  host: toolkit.lyrasistechnology.org
  app_port: 3000

servers:
  web:
    hosts:
      - toolkit.lyrasistechnology.org
    env:
      ADMIN_EMAILS: admin@core.collectionspace.org,admin@anthro.collectionspace.org
      SOLID_QUEUE_IN_PUMA: true
