<h5 class="mb-3">Manifests</h5>

<% if manifests.any? %>
  <div id="manifests">
    <%= render manifests %>
  </div>
<% else %>
  <div class="alert alert-info">
    No manifests have been loaded from this registry yet.
  </div>
<% end %>
